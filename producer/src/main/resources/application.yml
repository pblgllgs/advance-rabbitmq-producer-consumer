
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  application:
    name: producer

server:
  port: 8080

x:
  transaction: x.transaction
  scheduler: x.scheduler

q:
  transaction: q.transaction
  scheduler: q.scheduler
